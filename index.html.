<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TFA | Loading...</title>
    <style>
        /* Using a light grey background so the white logo is visible */
        body { background: #f0f0f0; margin: 0; display: flex; justify-content: center; align-items: center; height: 100vh; overflow: hidden; font-family: sans-serif; }
        .logo-container { text-align: center; }
        .bein-logo { width: 280px; height: auto; margin-bottom: 20px; }
        .loader { border: 4px solid #f3f3f3; border-top: 4px solid #4b0082; border-radius: 50%; width: 30px; height: 30px; animation: spin 1s linear infinite; margin: 0 auto; }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
    </style>
</head>
<body>
    <div class="logo-container">
        <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/1/1a/BeIN_Sports_logo.svg/2560px-BeIN_Sports_logo.svg.png" class="bein-logo">
        <div class="loader"></div>
        <p style="color: #4b0082; font-weight: bold; letter-spacing: 1px;">PREPARING BROADCAST...</p>
    </div>
    <script>
        // Redirecting to home.html after 3 seconds
        setTimeout(function() { window.location.href = "home.html"; }, 3000);
    </script>
</body>
</html>
