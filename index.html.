<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TFA | Loading...</title>
    <style>
        body { background: #ffffff; margin: 0; display: flex; justify-content: center; align-items: center; height: 100vh; font-family: sans-serif; }
        .bein-box { display: flex; align-items: center; border: 3px solid #4b0082; border-radius: 8px; font-weight: bold; font-size: 38px; }
        .bein-left { padding: 10px 20px; color: #4b0082; background: white; }
        .bein-right { padding: 10px 20px; color: white; background: #4b0082; }
    </style>
</head>
<body>
    <div style="text-align: center;">
        <div class="bein-box">
            <div class="bein-left">beIN</div>
            <div class="bein-right">TFA</div>
        </div>
        <p style="color: #4b0082; margin-top: 20px;">LOADING...</p>
    </div>
    <script>
        /* This moves the user to your dashboard automatically */
        setTimeout(function() { 
            window.location.href = "home.html"; 
        }, 3000);
    </script>
</body>
</html>
